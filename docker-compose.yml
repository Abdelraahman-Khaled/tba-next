services:
  tba-next:
    container_name: tba-next
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "9104:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
